{
  "name": "overloop-dashboard",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx wrangler pages dev .",
    "deploy": "npx wrangler pages deploy ."
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
